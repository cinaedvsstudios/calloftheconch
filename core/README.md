# Core code

`core` contains narrow services and data used by multiple app contexts. It should remain small. When code is used only by gameplay, a specific minigame, or one UI screen, keep it in that context or feature instead.
