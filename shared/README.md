# Shared code

Only put genuinely reusable, low-dependency modules here. A game-specific `MermaidMovementController`, `TreasureChest`, or `WhaleTravelSequence` belongs in its own feature folder, not in `shared` simply because it is a script.
