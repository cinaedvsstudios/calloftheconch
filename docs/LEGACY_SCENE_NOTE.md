# Legacy scene note

This clean project contains no `scenes/title/title_screen.tscn` and no `scenes/world/swim_test.tscn`.

If Godot still displays a missing-script error for `res://scripts/ui/title_screen.gd`, you are opening an old leftover scene from a previous project folder. Extract this ZIP into a new empty `Call_of_the_Conch_Prototype` folder instead of merging it over the older folder. `project.godot` starts only `app/RootContext.tscn`.
