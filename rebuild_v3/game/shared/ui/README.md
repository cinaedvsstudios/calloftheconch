# Shared UI

Ready scenes:

- `gameplay_hud.tscn`
- `inventory_overlay.tscn`
- `level_version_selector.tscn`

These wrappers point to the canonical UI scenes so layout and behaviour fixes remain shared across the project.
