# Whale Travel

Use `whale_travel.tscn` as the current reusable whale checkpoint and travel interaction scene.

Future departure and arrival cutscene scenes should be stored here by sea and selected by the travel controller rather than copied into every level.
