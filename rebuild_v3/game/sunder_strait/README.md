# Sunder-Strait

Content root for the Sunder-Strait.

Add local `levels`, `objects`, `enemies`, `bosses`, `effects` and `background_scenery` folders as production begins. Reuse completed shared scenes from `game/shared` rather than copying them.
